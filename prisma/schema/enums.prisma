enum ACTIVE_STATUS {
    ACTIVE
    INACTIVE
}

enum ROLE {
    USER
    ADMIN
    AUTHOR
}

enum PostStatus {
    PUBLISHED
    DRAFT
    ARCHIVED
}

enum CommentStatus {
    APPROVED
    REJECT
}

enum SubscriptionStatus {
    ACTIVE
    CANCELED
    EXPIRED
}
